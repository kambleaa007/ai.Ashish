Generated by AI. Make sure to check for accuracy.
Meeting notes:
Introduction to Model Context Protocol (MCP): 
nishi introduced the Model Context Protocol (MCP), explaining its purpose as a universal communication protocol between AI models and external data sources or tools, and described its client-server architecture to Shrikanth, Abhijeet, and Kajal.
	MCP Overview and Purpose: nishi explained that MCP stands for Model Context Protocol, an open standard created by Anthropic to act as a universal communication protocol between AI models and external data sources, tools, or applications. MCP provides a single, uniform language for large language models to securely read data, execute tasks, and interface with different software ecosystems without requiring custom-built code for each integration.
	Client-Server Architecture: nishi described the MCP's client-server architecture, where the client could be any AI application or interface (such as a cloud desktop or custom chatbot), and the server is a background process that sits atop specific apps or data sources, securely exposing their functionality to the AI.
	Supported Operations and Security: nishi emphasized that MCP servers can expose only safe operations to AI clients, such as listing database tables or executing select queries, while blocking potentially destructive commands like delete, update, drop, or insert, ensuring secure interaction with data sources.
Demonstration of MCP DB Inspector Project: 
nishi demonstrated the MCP DB Inspector project to Shrikanth, Abhijeet, and Kajal, showing how it exposes an in-memory SQLite database to AI assistants via MCP, and walked through its architecture, code structure, and safe operation limitations.
	Project Architecture and Components: nishi outlined the architecture of the MCP DB Inspector project, which consists of an MCP server implemented in TypeScript (server.ts) that exposes an in-memory SQLite database. The server registers two tools: one for listing database tables and another for executing select queries. Communication between the AI client and server occurs via JSON RPC over stdio.
	Safe Operations and Limitations: nishi clarified that the MCP server only allows listing tables and executing select queries, blocking all other SQL commands (delete, update, drop, insert) to prevent unsafe operations. The in-memory database is suitable for testing and learning, as all data is lost when the server stops.
	Code Structure and Dependencies: nishi explained that the main logic resides in server.ts, which uses libraries such as model context protocol SDK, SQLite3, and Zod for schema validation. The project structure includes configuration files like package.json and tsconfig.json, with TypeScript compiling the code to JavaScript.
	Live Demonstration and Querying: nishi demonstrated starting the MCP server, connecting it to a cloud desktop, and running queries such as listing tables, retrieving records, filtering error logs, and counting records. The AI client successfully interacted with the MCP server, showing the results of select queries on the system_logs table.
Troubleshooting and Build Issues with MCP Projects: 
nishi, with input from Abhijeet and Kajal, discussed and troubleshooted various build and configuration issues encountered while compiling and running MCP server projects, including TypeScript version mismatches, package.json script errors, and module resolution problems.
	Build Script and TypeScript Issues: nishi encountered errors related to npm scripts and TypeScript compilation, such as 'build' not being recognized, missing or misconfigured tsconfig.json, and version mismatches. Abhijeet suggested checking and removing problematic scripts like 'rm -rf' from package.json, and verifying the TypeScript version.
	Module Resolution and Directory Structure: nishi and Kajal discussed issues with module resolution, such as 'Cannot find module dist/index.js', and clarified the expected directory structure, including the use of src as the root directory and dist as the output directory for compiled files.
	Collaborative Troubleshooting: Abhijeet and Kajal provided suggestions and confirmed their own setups were working, helping nishi identify that the issues were likely local to nishi's environment rather than with the MCP project code itself.
Demonstration of Support Ticket MCP Project: 
nishi presented an intermediate MCP project for support ticket management, explaining its architecture, tool registration, and business logic, and described how AI assistants can interact with the backend via MCP tools.
	Project Structure and Business Logic: nishi described the support ticket MCP project, which includes files such as data.ts (containing customer and ticket arrays and business logic functions), tool.ts (registering MCP tools and defining their schemas), and index.ts (the application entry point that starts the MCP server and registers tools).
	Tool Registration and AI Interaction: nishi explained that seven MCP tools are registered, including list_ticket, get_ticket, search_ticket, and list_customer. These tools expose business operations to the AI assistant, allowing it to perform actions like listing urgent tickets or retrieving customer information.
	End-to-End Flow: nishi outlined the end-to-end flow: a customer raises a ticket via a support portal, the backend API processes the request, and the MCP server exposes the relevant operations to the AI assistant, which can then interact with the ticketing system using plain English queries.
Hands-On Testing and Peer Collaboration: 
Kajal shared their screen to demonstrate a working MCP server connection and successful tool invocation, while nishi encouraged participants to try the project code themselves and report back on their results.
	Screen Sharing and Live Testing: Kajal demonstrated a working MCP server setup by sharing their screen, showing successful connection to the MCP server, querying available tools, and executing project-related queries such as reading files and performing arithmetic operations.
	Encouragement for Independent Practice: nishi provided the project code and encouraged all participants to try running the MCP projects on their own machines, offering to review any issues or results in the next session.
Follow-up tasks:
Build Issues Troubleshooting: 
Investigate and resolve the MCP server connection and build issues experienced by Nishi, including module not found and config problems, and report findings to the group. (nishi)
Project Code Sharing and Testing: 
Share the MCP project code files (data.ts, index.ts, tool.ts) with the team and request members to try running the project on their machines, then provide feedback on functionality by the next meeting. (nishi, the team)